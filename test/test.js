function assert(value, description) {
    if (!value) {
        throw new Error(description + "is Failed !");
    }
}

assert(5 + 6 === 12, "5 + 6 is 11!");

var sum - function(a, b) {
    return a + b;
}